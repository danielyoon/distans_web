const LOGIN = {
  SUCCESS: "SUCCESS",
  WRONG: "WRONG",
  EXPIRED: "EXPIRED",
  NONEXISTENT: "NONEXISTENT",
};

const ROLE = {
  Admin: "Admin",
  User: "User",
};

const CHECK = {
  IN: "IN",
  OUT: "OUT",
};

module.exports = { LOGIN, ROLE, CHECK };
